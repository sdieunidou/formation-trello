{% extends 'base.html.twig' %}

{% trans_default_domain('app') %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>{{ 'task_edit.title'|trans({'%name%': task.name}) }}</h1>

                {% include ':task:_form.html.twig' %}
            </div>
        </div>
    </div>
{% endblock %}
