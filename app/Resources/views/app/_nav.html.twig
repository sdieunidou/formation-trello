{% trans_default_domain('layout') %}

<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('app_category_list') }}">{{ 'app.navigation.categories_list'|trans }}</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <div class="navbar-form navbar-right">
                <div class="btn-group">
                    <a href="{{ path('app_category_list') }}" type="button" class="btn btn-default btn-sm">
                        {{ 'app.navigation.categories_list'|trans }}
                    </a>
                    <a href="{{ path('app_task_new') }}" type="button" class="btn btn-primary btn-sm">
                        {{ 'app.navigation.task_create'|trans }}
                    </a>
                </div>
            </div>
        </div>
    </div>
</nav>
